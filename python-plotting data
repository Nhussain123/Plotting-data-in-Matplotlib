# Creating line plots with Matplotlib
Line plot uses a co-ordinate gridi to plot a series of points then connecting each

To create a line plot
  from matplotlib import pyplot as plt
  
  Function
  plt.plot(x_values, y_values)
  plt.show()
  
  plt.plot(ransom.letter, ransom.frequency)
            dataframe name - column name
            
  plt.show() - shows graph
  
  to add second line just add below or multiple just add a second plt.plot
  
  plt.plot(data1.x_values,
            data1.y_values)
  plt.plot(data2.x_values,
            data2.y_values)
  plt.show()

#Adding labels and legends

--- Axes and title labels ---
plt.xlabel("Letter")
plt.ylabel("Frequency")
plt.title("Ransom Note Letters")

--- Legends - label at each plt.plot ---
plt.plot(aditya.days,
         aditya.cases,
         label="Aditya")
plt.legend() - Must add to show legend

--- Arbitrary text ---
plt.text(xcoord,ycoord,"Text Message")
To add in specific place for eg
plt.text(5,9,"insert text")

--- Modififying text ---

--- Change font ---
plt.title("Plot title", fontsize=20)

--- Change font color ---
plt.legend(color="green")

--- Adding style ---
Changine line color
plt.plot(x,y1,color="tomato")

--- Width line ---
plt.plot(x, y1, linewidth=1)

--- Changing line style ---
plt.plot(x, y1, linestyle='_, --, -., :'

--- Adding markers---
plt.plot(x, y1, marker='x, s, a, d, *, h')

--- Select a style ---
plt.style.use('fivethirtyeight')
plt.style.use('ggplot')
plt.style.use('seaborn')
plt.style.use('default')

How to make a scatterplot
